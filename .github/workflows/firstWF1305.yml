on:
  push:
    branches:
    - main
    - dev
    - release
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: run a one line script
        run: echo hello, world!

on:
  workflow_dispatch:
    
jobs:
  callworkflow:
    runs-on: ubuntu-latest

    steps:
      - name: Print the input name to STDOUT from called
        uses: ./.github/workflows/workflowcall.yml
        with:
          username: 'hellop'
